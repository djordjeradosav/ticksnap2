/** @type {const} */
const themeColors = {
  // Light theme colors (all screens use light theme)
  primary: { light: '#F5C518', dark: '#F5C518' },      // Yellow accent
  background: { light: '#FFFFFF', dark: '#FFFFFF' },  // White background
  surface: { light: '#F5F5F5', dark: '#F5F5F5' },     // Light gray cards
  foreground: { light: '#000000', dark: '#000000' },  // Black text
  muted: { light: '#888888', dark: '#888888' },       // Gray secondary text
  border: { light: '#E5E5E5', dark: '#E5E5E5' },      // Light border
  success: { light: '#00C087', dark: '#00C087' },     // Green (profit)
  warning: { light: '#F59E0B', dark: '#F59E0B' },     // Amber
  error: { light: '#FF4444', dark: '#FF4444' },       // Red (loss)
  accent: { light: '#F5C518', dark: '#F5C518' },      // Yellow accent
};

module.exports = { themeColors };
